# 코드 점검 메모

1. **OpenAI 클라이언트 호환성 (chatbot_v3.py:120-133)**  
   - 문제: `openai.ChatCompletion.create`는 구버전 전용 API라 `openai` 패키지 v1.x에서 `AttributeError`가 발생해 분류기가 항상 폴백 로직으로만 동작했습니다.  
   - 조치: `from openai import OpenAI` 클라이언트를 도입하고 `client.chat.completions.create(...)` 패턴으로 교체, 응답 파싱도 가드 로직을 추가했습니다.

2. **추천 개수 제한 버그 (chatbot_v3.py:1039-1143)**  
   - 문제: 후보 문서를 `docs[:1]`로 절단하고 중복 제거 루프에서 즉시 `break`하여 추천 결과가 항상 1건으로 고정됐습니다.  
   - 조치: 상위 3건을 유지하도록 슬라이싱과 루프 조건을 조정하고, 보충 로직도 최대 3건까지 채우도록 수정했습니다.

3. **나이 미입력 시 과도한 필터링 (chatbot_v3.py:722-742, 1046-1060)**  
   - 문제: 나이가 미입력된 상태에서 `user_age`를 0으로 처리해 모든 정책이 연령 필터에서 탈락, 지역·관심사만으로는 추천이 불가능했습니다.  
   - 조치: `filter_docs`가 `Optional[int]`를 받아 나이가 없을 땐 조건을 건너뛰고, 호출부에서는 `None`을 그대로 전달하도록 변경했습니다.

4. **정책 질의 분류 오판 (chatbot_v3.py:101-118)**  
   - 문제: 나이·지역·관심사를 동시에 제공한 자연어 문장을 LLM 분류기로 넘기면 ‘정책 질문 아님’으로 잘못 분류되어 대화가 진행되지 않았습니다.  
   - 조치: 나이·지역·관심 표현, 정책 키워드, `parse_user_input` 결과를 기반으로 한 선행 휴리스틱을 추가해 명백한 정책 질의를 즉시 승인하도록 보강했습니다.

5. **벡터스토어 미구성 시 예외 (chatbot_v3.py:885-1140)**  
   - 문제: 콘솔 모드에서 `policy_vectordb`가 로드되지 않은 경우 `raw_docs` 미정의, `None` 객체 호출 등으로 예외가 발생하거나 사용자가 원인 없이 중단을 경험했습니다.  
   - 조치: 벡터스토어가 비어 있으면 사용자에게 안내 후 종료하고, `raw_docs`를 기본값으로 초기화해 후속 로직에서 안전하게 참조하도록 수정했습니다.

6. **관심사 필터 정확도 (chatbot_v3.py:895-1108)**  
   - 문제: 세부 관심사 질문에 자연어로 답하면 해당 문장을 그대로 `stored_interests`에 넣어 벡터 필터가 작동하지 않았고, 결과적으로 전국 공통 정책만 반환됐습니다.  
   - 조치: 관심사 업데이트를 전용 헬퍼로 통합해 나이·지역을 재추출하고, 인터랙션에서 얻은 텍스트를 상위 카테고리로 표준화해 필터가 실제 데이터와 매칭되도록 했습니다.

7. **맞춤 정보 수집 흐름 미흡 (chatbot_v3.py:960-1305, 1383-1472)**  
   - 문제: 초기 구현은 기본 정보가 부족해도 즉시 검색을 시도하고 JSON만 출력해 사용자가 어떤 정보를 더 제공해야 하는지 알기 어려웠습니다. 또한 추가 변수(소득, 고용 상태, 희망 직무 등)를 질문하지 않아 맞춤 추천 정밀도가 떨어졌습니다.  
   - 조치: 필수·선택 정보를 구분해 단계적으로 질문하고, 조건 충족 시 결과 개수·확인 프로프트를 제공하도록 콘솔 플로우를 재작성했습니다. 동일한 추가 변수들을 서버 API에서도 검색 질의에 반영해 다중 채널에서 일관된 정밀 추천이 가능하도록 보강했습니다.

8. **지역 별칭 누락으로 인한 매핑 실패 (chatbot_v3.py:333-757)**  
   - 문제: 홍대·압구정·판교 같은 생활권 키워드나 ‘○○동’ 입력이 공식 행정 구역명과 달라 인식되지 않아 지역 조건이 비어 버렸습니다.  
   - 조치: 주요 생활권 별칭 테이블을 추가하고, 시·군·구·동·읍·면 접미사를 자동으로 절단하는 로직을 확장했으며, 별칭과 원문을 모두 역매핑 테이블에 병합해 다양한 표현을 동일 표준 지역으로 변환합니다.
